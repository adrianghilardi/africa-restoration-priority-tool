# Project-only library discovery for the documented standalone Rscript commands.
# Commands are run from this code directory; no global R settings are modified.
if(dir.exists('.r-library')) .libPaths(c(normalizePath('.r-library'), .libPaths()))
