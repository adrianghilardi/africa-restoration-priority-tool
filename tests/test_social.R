source("R/social_core.R")
stopifnot(identical(social_proxy(c(100,0,NA,100),c(50,50,50,NA)),c(50,0,NA_real_,NA_real_)))
stopifnot(identical(social_proxy(c(100,100),c(0,100),.5),c(0,50)))
stopifnot(all(is.na(social_proxy(c(-1,1,Inf),c(50,101,10)))))
stopifnot(social_weighted_rate(c(100,300),c(20,60))==50)
stopifnot(is.na(social_weighted_rate(c(0,0),c(20,60))))
stopifnot(is.na(social_weighted_rate(c(100,300),c(NA,NA))))
stopifnot(inherits(try(social_proxy(1,c(1,2)),silent=TRUE),"try-error"))
stopifnot(inherits(try(social_proxy(1,1,2),silent=TRUE),"try-error"))
cat("8 social core test groups passed\n")
